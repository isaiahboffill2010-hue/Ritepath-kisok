{
  "name": "ritepath-kiosk-desktop",
  "private": true,
  "type": "module",
  "main": "desktop/main.mjs",
  "scripts": {
    "dev:ui": "npm --prefix frontend run dev",
    "build:ui": "npm --prefix frontend run build",
    "dev:backend": "python -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000",
    "desktop:dev": "node desktop/dev.mjs",
    "desktop:start": "node desktop/main.mjs"
  },
  "devDependencies": {
    "electron": "43.2.0"
  }
}
